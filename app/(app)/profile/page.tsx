import { Card } from "@/components/ui/card";

// 🛠️ Stub — Session 5 builds the Red Book, psych profile, comm-profile bars, privacy toggles.
export default function ProfilePage() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-bold text-ink">🌟 Profile &amp; Red Book</h1>
      <Card>
        <p className="text-sm text-muted">
          Session 5 builds identity here — your Red Book (North Star goals), the psychological
          profile, the live communication-profile bars, places, and privacy toggles.
        </p>
      </Card>
    </div>
  );
}
